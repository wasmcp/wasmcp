[package]
name = "dev-tools"
version = "0.1.0"
edition = "2024"
publish = false
autobins = false

# Not a workspace member - builds natively for host platform
[workspace]

[[bin]]
name = "dev-tools"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
toml_edit = "0.22"
toml = "0.8"
walkdir = "2.5"
serde = { version = "1.0", features = ["derive"] }
pathdiff = "0.2"
