manifest_version = "1"
id = "ftl-javascript"
description = "FTL JavaScript MCP tool"
tags = ["mcp", "http", "javascript", "ftl"]

[parameters]
project-description = { type = "string", prompt = "Description", default = "An MCP tool written in JavaScript" }
route = { type = "string", prompt = "HTTP route", default = "/mcp" }

[add_component]
skip_files = ["spin.toml", "README.md", ".gitignore", "ftl.toml"]
[add_component.snippets]
component = "component.txt"