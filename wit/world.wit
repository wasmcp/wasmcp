package fastertools:mcp@0.1.1;

/// World for MCP implementations that only provide tools
world mcp-tools {
    import types;
    import session;
    import tools;
    
    export core;
    export tool-handler;
}

/// World for MCP implementations that only provide resources
world mcp-resources {
    import types;
    import session;
    import resources;
    
    export core;
    export resource-handler;
}

/// World for MCP implementations that only provide prompts
world mcp-prompts {
    import types;
    import session;
    import prompts;
    
    export core;
    export prompt-handler;
}

/// World for MCP implementations that provide tools and resources
world mcp-tools-resources {
    import types;
    import session;
    import tools;
    import resources;
    
    export core;
    export tool-handler;
    export resource-handler;
}

/// World for full MCP server implementations
world mcp-full {
    import types;
    import session;
    import tools;
    import resources;
    import prompts;
    
    export core;
    export tool-handler;
    export resource-handler;
    export prompt-handler;
}