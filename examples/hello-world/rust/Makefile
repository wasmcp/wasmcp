# Makefile for rust

.PHONY: work setup build

# Default target - full workflow
work: setup build

# Setup (Rust has no setup step - cargo handles everything)
setup:

# Build the Rust component
build:
	@cargo component build --release
