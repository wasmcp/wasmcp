[package]
name = "writer"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen = "0.34"
serde_json = "1.0"
base64 = "0.22"

[features]
default = ["http"]
http = []
stdio = []

[lib]
crate-type = ["cdylib"]
