[package]
name = "stdio-transport"
version = "0.1.0"
edition = "2021"

[dependencies]
wit-bindgen = "0.34"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"

[dev-dependencies]
serde_json = "1.0"

[features]
integration-tests = []

[lib]
crate-type = ["cdylib"]
