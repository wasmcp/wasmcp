[package]
name = "server-io"
version = "0.1.4"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wit-bindgen = "0.48"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.22"
