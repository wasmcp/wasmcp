name: Release Filter Middleware

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Component version (e.g. 0.1.0 or 0.1.0-beta.2)'
        required: true
        type: string

jobs:
  release:
    name: Release Filter Middleware
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/reusable-release-component.yml
    with:
      component-name: filter-middleware
      component-crate: filter-middleware
      version: ${{ inputs.version }}
      wkg-package: wasmcp:filter-middleware
      tag-prefix: filter-middleware-v
    secrets: inherit
