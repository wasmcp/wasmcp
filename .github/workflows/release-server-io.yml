name: Release Server IO

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Component version (e.g. 0.1.0 or 0.1.0-beta.2)'
        required: true
        type: string

jobs:
  release:
    name: Release Server IO
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/reusable-release-component.yml
    with:
      component-name: server-io
      component-crate: server-io
      version: ${{ inputs.version }}
      wkg-package: wasmcp:server-io
      tag-prefix: server-io-v
    secrets: inherit
